export type Product = {
  id: string; name: string; slug: string;
  brand?: string; category: string; price?: number; images?: string[];
}
export type News = { id: string; title: string; excerpt: string; image?: string }
